#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LAUNCHER_DIR="$SCRIPT_DIR/src/entry"

if [ "$#" -eq 0 ]; then
    exec python3 "$LAUNCHER_DIR/main_visual.py"
fi

exec python3 "$LAUNCHER_DIR/main.py" "$@"
